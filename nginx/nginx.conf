events {
    worker_connections 100;
}

http {
    # Use Docker embedded DNS for service name resolution inside the container
    resolver 127.0.0.11 ipv6=off;
    server{
        listen 80;
        location / {
            proxy_pass http://app:8081;
        }
    }
}