# Читалка

Сервис для помощи в чтении книг и статей.

## Запуск проекта

Проект запускается через Docker Compose.

### Требования

- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

### Подготовка конфигурации

1. В корне проекта создайте файл `.env` на основе `.env.example` (переменные для docker-compose: PostgreSQL, RabbitMQ, порты и т.д.).
2. В каталоге `app/` создайте файл `.env` на основе `app/.env.template` (настройки приложения: JWT, подключение к БД и RabbitMQ).

### Запуск

Из корня проекта выполните:

```bash
docker-compose up
```

Для запуска в фоновом режиме:

```bash
docker-compose up -d
```

После старта будут подняты сервисы:

| Сервис      | Описание                    | Доступ                          |
|-------------|-----------------------------|----------------------------------|
| **web-proxy** | Nginx (веб-интерфейс и API) | http://localhost:80             |
| **app**       | Backend API                 | через Nginx на порту 80         |
| **ml_worker** | ML-воркеры (2 реплики)      | внутренняя сеть                 |
| **ollama**    | LLM для обработки текста    | внутренняя сеть                 |
| **database**  | PostgreSQL                  | localhost:5432                  |
| **rabbitmq**  | Очередь задач               | AMQP 5672, веб-интерфейс :15672 |

Приложение доступно в браузере по адресу **http://localhost** (или порт из `NGINX_HTTP_PORT` в `.env`).

### Остановка

```bash
docker-compose down
```

С опцией `-v` удаляются тома (данные БД, RabbitMQ, Ollama):

```bash
docker-compose down -v
```

## Функциональные требования к личному кабинету

1. Главная страница с описанием возможностей ML сервиса
2. Возможность зарегистрироваться и авторизоваться
3. Возможность просматривать баланс (баланс в условных кредитах)
4. Возможность пополнять баланс через кнопку пополнить (эквайринг подключать не требуется)
5. Возможность выполнить запрос к ML сервису из Web интерфейса со списанием кредитов с баланса (при условии успешного выполнения запроса)
6. Возможность просматривать историю загруженных данных и предсказаний по ним, с указанием суммы списанных кредитов
7. Возможность выполнять запросы к ML сервису с помощью REST интерфейса
8. Требуется предусмотреть валидацию данных. Если в загружаемой выборке присутствуют ошибочные данные, то возвращать их пользователю, а над валидными данными производить предсказание
9. Требуется предусмотреть проверку на положительный баланс при запросах к системе.
10. Роль администратор подразумевает возможность пополнять баланс пользователям (модерировать пополнения от пользователей) и просматривать все транзакции.

## Функциональные возможности сервиса

1. Регистрация и логин пользователя
2. Выбор уровня английского (A1-B2)
3. Загрузка текста: txt / epub
4. Обработка текста
5. Перевод


## Связи между сущностями

### 1. User ↔ Balance
- **Связь**: Один к одному
- **Описание**: Каждый пользователь имеет один баланс
- **Реализация**: `Balance._user_id` ссылается на `User._id`

### 2. User ↔ TextDocument
- **Связь**: Один ко многим
- **Описание**: Пользователь может загрузить множество документов
- **Реализация**: `TextDocument._user_id` ссылается на `User._id`

### 3. TextDocument ↔ Task
- **Связь**: Один к одному
- **Описание**: На обработку документа может быть создана задача
- **Реализация**: `Task._document_id` (опционально) ссылается на `TextDocument._id`

### 4. User ↔ Transaction
- **Связь**: Один ко многим
- **Описание**: Пользователь может иметь множество транзакций
- **Реализация**: `Transaction._user_id` ссылается на `User._id`

### 5. Task ↔ Prediction
- **Связь**: Один к одному
- **Описание**: По одной задаче возможно одно решение (предсказание)
- **Реализация**: `Prediction._task_id` ссылается на `Task._id`


## Поток работы системы

### 1. Регистрация пользователя
```
User (создание) → User.validate() → Balance (создание с начальным балансом 0)
```

### 2. Загрузка документа
```
User → TextDocument (создание) → TextDocument.validate() → сохранение
```

### 3. Создание задачи обработки
```
User → Task (создание с document_id) → Task.validate() → проверка Balance.has_sufficient_balance() → Transaction (списание) → Balance.withdraw()
```

### 4. Выполнение задачи
```
Task → Task.set_status(PROCESSING) → обработка → Task.set_result() → Task.set_status(COMPLETED) → Prediction (создание из task.result)
```

### 5. Пополнение баланса
```
User → Transaction (REPLENISHMENT) → Transaction.validate() → Balance.replenish()
```

### 6. Обработка ошибки задачи
```
Task → Task.set_error() → Task.set_status(FAILED)
Примечание: Транзакция уже создана при отправке задачи, средства уже списаны
```